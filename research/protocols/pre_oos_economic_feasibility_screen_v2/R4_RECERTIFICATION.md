# R4 recertification — separate prerequisite to protected OOS

Procedure only. No rerun, protected-data read or original-artifact modification authorized here. This recertification is mandatory BEFORE protected OOS authorization, not a prerequisite for the owner's newly authorized pre-OOS feasibility protocol preparation.

## Facts to retain separately

- Historical R4 may have opened two 2026 files in detection/loading before date filtering. A hardcoded `protected_2026_untouched` flag is not a no-read proof.
- No current evidence establishes that those files contributed to calculation of the 500 published survivors. This is not itself a proof of absence of contribution.
- The four eligible datasets identified by existing artifacts and all 500 candidate dataset fields are certified pre-2026 XAUUSD M1/M5 files; their current hashes matched the Phase I-B hashes in the earlier audit.
- Preserve the published 500 and their raw/canonical hashes. No destruction, threshold changes or automatic replacement of the population.

## Route A — static/dataflow certificate

Review the exact engine at receipt commit 63b825bad510150225154c8f539d14e025d8c8d9, dependency versions, original inventory order, seed 260912 and CLI parameters. Trace file detection → date filtering → year-count eligibility → markets array → features → RNG consumption → selected/confirmation/neighbor arrays → published rule fields.

Establish using existing manifests/receipts/logs, WITHOUT reopening protected files, that the two extra entries could not append a market, change RNG consumption, threshold fitting, candidate counts, features, filters, ordering or publication. Inspect global/import/cache side effects and the pre-filter max_rows operation. Document where original exception logs or immutable content proofs are absent; do not substitute filenames or a six-scanned/four-eligible count alone for proof. Compare all four eligible path/hash/order entries and source signatures against the publication.

PASS route A only if the evidence closes every influence path to economic/statistical inputs. The certificate may prove non-contribution, not undo historical file reads. If a required premise remains unknown, record INSUFFICIENT_PROOF and propose route B rather than claiming PASS.

## Route B — separately preregistered reproducibility rerun

Requires separate authorization and duplicate-process check. Isolated immutable engine revision may replace only source discovery with the four exact allowlisted snapshots in the ORIGINAL eligible order. Keep seed, 250000 trials, numerical environment, all R4 scientific constants, filtering and cap/order semantics unchanged. No future market directory is accessible to the process. Pre-register this diff and comparison before execution; do not relax comparisons after mismatch.

Write only new output/receipt/progress paths. Compare the 500 canonical signal signatures exactly, including cutpoints and array order; compare scientific result fields and source-dataset identities. Exclude only documented generation timestamps and infrastructure provenance from full-result comparison; do not introduce a numerical tolerance to excuse a changed signature. Verify all four input hashes and record both runs' executable/environment hashes.

Mismatch => investigate scientific/numerical/provenance difference, preserve both runs, and block OOS. A match establishes reproducibility using exclusively admitted pre-2026 inputs; it does not erase prior access or automatically validate statistical methodology. The prior HAC/year-boundary audit limitations remain recorded.

Neither route automatically changes the 500 population, starts a protected job or grants OOS permission. A separate finalist freeze, realistic-backtest decision, cold audit and explicit owner authorization remain necessary.
